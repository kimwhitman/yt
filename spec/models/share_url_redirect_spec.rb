require 'spec_helper'

describe ShareUrlRedirect do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ShareUrlRedirect.create!(@valid_attributes)
  end
end
