  
      <div class="form_row">
        <div class="video_list_thumb">
					<%= link_to_video video %>
        </div>
        
        <div style="float: left;" class="grid_spacer"> </div>
        <div style="float: left;" id="container_3col">
					<%= link_to_video video, :text => video.title, :class => 'video-thumb-title' %><br />          
          <span class='video-thumb-info'><%= video.instructor_names.join(', ') %> | <span><%= video.skill_name %></span> | <%= video.duration_to_minutes %>:<%= video.duration_seconds %></span><br/>
					<%= video.description %>          
        </div>
        
        <div style="width: 160px; float: left;" id="container_1col">
					<%= link_to_video video, :text => 'Details', :class => 'button', :style=> 'margin-right: 10px;' %>
          <%= link_to_function "Remove", "$('#remove_options_#{video.id}').show()", :id => "remove_button_#{video.id}", :class => "button" %>
          <div id="remove_options_<%=video.id%>" style="display:none; text-align: center; background-color: #dddddd; border: 1px solid #999999; width: 51px; float: right; margin-top: 2px; margin-right: 20px;">
            <%= link_to_remote 'Yes', :url => {:controller => "playlists", :action => "remove", :video_id => video.id}, :html => {:style => "margin-left: 1px;"} %> /
            <%= link_to_function "No", "$('#remove_options_#{video.id}').hide()", :style => "padding-right: 1px" %>
          </div>
        </div>
        
        <div style="float: right;" id="container_1col">
					<%= add_video_to_cart_link "Buy | #{number_to_currency(video.price(current_user))}", video %>
        </div>
              </div>
